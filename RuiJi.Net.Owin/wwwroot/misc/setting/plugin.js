define(['jquery', 'utils'], function ($, utils) {
    var module = {
        init: function () {
            $("#tab_panel_plugin").html("<h3>Comming Soon!</h3>");
        }
    };

    module.init();
});